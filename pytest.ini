[pytest]
testpaths = tests
markers =
    unit: deterministic in-process contract checks
    store: real workspace or SQLite persistence checks
    cli: command-rendering checks
    integration: crosses package boundaries with scripted backends
    e2e: governed control-loop acceptance checks
    conformance: static architecture, documentation, and inventory checks
    slow: subprocess, multiprocessing, or otherwise expensive checks
